# 🔥 GitRoast

**GitRoast** is a brutal, AI-powered web application that roasts GitHub profiles. It uses the Groq SDK (Llama 3 70B) to generate unique, savage, and hilarious critiques of GitHub users based on their public data.

## ✨ Features

- **Brutal AI Roasts**: Generates savage commentary on bio, repo count, languages, and followers using Groq's fast inference.
- **GitHub Integration**: Fetches real-time data (avatar, bio, stats, repositories) via the GitHub API.
- **Dynamic Personas**: The AI adopts different toxic personas (e.g., "Rage-filled Senior Dev", "Unhinged Maintainer") for every request.
- **Flat UI Design**: A clean, modern, dark-themed interface with no gradients, focusing on the content.
- **Responsive**: Works seamlessly on desktop and mobile.

## 🛠️ Tech Stack

- **Backend**: Python, Flask
- **AI/LLM**: Groq SDK (GPT OSS 120B)
- **Dependency Management**: [uv](https://github.com/astral-sh/uv)
- **Frontend**: HTML5, CSS3 (Custom Flat Dark Theme)

## 🚀 Getting Started

### Prerequisites

- Python 3.12+
- [uv](https://github.com/astral-sh/uv) (recommended for fast dependency management)
- A [Groq API Key](https://console.groq.com/)
- A [GitHub Personal Access Token](https://github.com/settings/tokens) (optional, but recommended for higher rate limits)

### Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/yourusername/gitroast.git
   cd gitroast
   ```

2. **Install dependencies**
   ```bash
   uv sync
   # OR with pip
   pip install -r requirements.txt
   ```

3. **Set up Environment Variables**
   Create a `.env` file in the root directory:
   ```bash
   touch .env
   ```
   Add the following keys:
   ```env
   GROQ_API_KEY=your_groq_api_key_here
   GITHUB_TOKEN=your_github_pat_here
   PORT=8080
   ```

4. **Run the Application**
   ```bash
   uv run python app.py
   ```
   The app will start at `http://localhost:8080`.

## 📝 Environment Variables

| Variable | Description | Required |
|----------|-------------|----------|
| `GROQ_API_KEY` | Your API key from Groq Console. | Yes |
| `GITHUB_TOKEN` | GitHub Personal Access Token (Classic). Increases API rate limit to 5000/hr. | No (Recommended) |
| `PORT` | Port to run the Flask server on (default: 5000). | No |

## ⚠️ Disclaimer

This application is for **entertainment purposes only**. The roasts are generated by an AI instructed to be "brutal" and "abusive" in a fictional context. Do not take them seriously.

## 📄 License

MIT
